language: node_js

node_js: stable

install:
  - npm install

script:
  - hexo cl
  - hexo g

after_script:
  - cd ./public
  - git init
  - git config user.name "HelloWood"
  - git config user.email "hellowoodes@gmail.com"
  - git add .
  - git commit -m "update"
  - git push --force --quiet "https://${GH_TOKEN}@${GH_REF}" master:master


env:
 global:
   - GH_REF: github.com/helloworlde/helloworlde.github.io.git

